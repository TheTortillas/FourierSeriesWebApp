// Interfaz para manejar cada pieza de la función por tramos
export interface Piece {
  funcField: any; // MathField de la parte "func"
  startField: any; // MathField de la parte "start"
  endField: any; // MathField de la parte "end"
}
